meta:
  environment: node
  toolchain:
    name: npm
    version: 18

build:
  artifacts:
    - "server.js"
    - "package.json"
    - "package-lock.json"

run:
  command: npm start
  persistence:
    - name: uploads
      mount: /app/uploads
  ports:
    - port: 3000
      http_port: 80